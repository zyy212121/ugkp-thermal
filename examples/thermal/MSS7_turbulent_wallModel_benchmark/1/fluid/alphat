FoamFile { version 2.0; format ascii; class volScalarField; object alphat; }
dimensions [1 -1 -1 0 0 0 0];
internalField uniform 0.001;
boundaryField {
 inlet { type calculated; value uniform 0.001; }
 outlet { type calculated; value uniform 0.001; }
 fluid_to_graphite { type compressible::alphatJayatillekeWallFunction; Prt 0.85; value uniform 0.001; }
 wedgeBack { type wedge; }
 wedgeFront { type wedge; }
 defaultFaces { type empty; }
}
